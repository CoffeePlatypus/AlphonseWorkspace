
public class StartsWith implements Filter {
	private String pre;
	public StartsWith<E>(String s) {
		pre=s;
	}
	public boolean accepts(Object e) {
		return e.toString().toUpperCase().startsWith(pre.toUpperCase());
	}

}
